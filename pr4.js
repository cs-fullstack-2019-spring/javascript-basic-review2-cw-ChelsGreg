//
// //
// // //Create a function that returns 5 more than the integer sent to it.
// // // Assume the user only enters integers.


    var fiveMore = parseInt(fiveMore);

    function addTo(fiveMore)
{
     return (fiveMore + 5);
}

    console.log(addTo(9));


//
// // // Create a different function that adds " is awesome" to any strings sent to it.


    var addString = (" ");

    function isAwesome(addString)

{
    console.log(addString + " is awesome!");
}

isAwesome("Chelsea");




// Challenge: If the wrong data type is sent to one of the functions print "ERROR".



// var fiveMore = (fiveMore);
//
//
// function addTo(fiveMore)
// {
//    while (!isNan(fiveMore))
//    { console.log("Error"); }
//
//     else { return (fiveMore + 5); }
// }
//
// console.log(addTo("thirty"));